import {NextRequest,NextResponse} from 'next/server';
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get('code');const clientId=process.env.DISCORD_CLIENT_ID;const secret=process.env.DISCORD_CLIENT_SECRET;const redirect=process.env.DISCORD_REDIRECT_URI;if(!clientId||!secret||!redirect)return NextResponse.json({error:'Discord OAuth env vars missing'},{status:500});if(!code){const u=new URL('https://discord.com/oauth2/authorize');u.searchParams.set('client_id',clientId);u.searchParams.set('response_type','code');u.searchParams.set('redirect_uri',redirect);u.searchParams.set('scope','identify');return NextResponse.redirect(u)}const body=new URLSearchParams({client_id:clientId,client_secret:secret,grant_type:'authorization_code',code,redirect_uri:redirect});const t=await fetch('https://discord.com/api/oauth2/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});if(!t.ok)return NextResponse.json({error:'OAuth failed'},{status:401});const tok=await t.json();const me=await fetch('https://discord.com/api/users/@me',{headers:{authorization:`Bearer ${tok.access_token}`}});const user=await me.json();const res=NextResponse.redirect(new URL('/dashboard',req.url));res.cookies.set('camelliera_user',JSON.stringify({id:user.id,username:user.username,avatar:user.avatar}),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:60*60*24*7,path:'/'});return res}
